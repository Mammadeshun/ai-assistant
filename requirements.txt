# Runs on the server (Linux) and on your PC.
# Windows-only extras live in requirements-windows.txt.

requests>=2.31
schedule>=1.2

# Kiro scraper
selenium>=4.15
webdriver-manager>=4.0
beautifulsoup4>=4.12

# Volume-tier models go through 9router (see modules/llm.py), which speaks
# plain HTTP, so no provider SDK is needed here.

# Gmail
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1
